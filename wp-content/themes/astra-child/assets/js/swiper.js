document.addEventListener('DOMContentLoaded', () => {
    // const el = document.querySelector('.qz-gallery-swiper');
    // if (!el) return;
    // if (typeof Swiper === 'undefined') return;
  
    // new Swiper(el, {
    //   // 設定放這
    //   slidesPerView: 1,
    //   spaceBetween: 12,
    //   loop: false,
    // });
  });
  